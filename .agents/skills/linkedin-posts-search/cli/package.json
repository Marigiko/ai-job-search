{
  "name": "linkedin-posts-cli",
  "version": "1.0.0",
  "description": "CLI to extract apply-by-email job leads from public LinkedIn post permalinks (or pasted text) — no authentication, zero runtime dependencies. Personal use only (LinkedIn ToS).",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "linkedin-posts-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 30000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "latest"
  }
}
